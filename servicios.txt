<!-- <section class="service-section py-5">
    <div class="container">
        <div class="section-header text-center mb-4">
            <h2 class="section-title">Nuestros Servicios</h2>
            <div class="section-underline"></div>
        </div>

        <div class="row gy-4">
            @forelse ($services as $service)
                <div class="col-12">
                    <div class="service-card animate-on-scroll {{ $loop->even ? 'animate__fadeInLeft' : 'animate__fadeInRight' }} d-flex flex-column flex-md-row shadow-sm rounded p-3">


                        {{-- Imagen --}}
                        <div class="service-image-container flex-shrink-0 mb-3 mb-md-0">
                            @if ($service->image)
                                <img src="{{ $service->image }}" alt="{{ $service->name }}" class="img-fluid rounded service-image">
                            @endif
                        </div>

                        {{-- Información --}}
                        <div class="service-content ms-md-4">
                            <h4 class="fw-bold mb-2">{{ $service->name }}</h4>
                            <p class="text-muted mb-3">{{ Str::limit($service->description, 150) }}</p>
                            <a href="{{ route('dashboard.services') }}" class="btn btn-outline-primary">
                                Más Información →
                            </a>
                        </div>
                    </div>
                </div>
            @empty
                <div class="col-12 text-center text-muted">
                    <p>No hay servicios disponibles en este momento.</p>
                </div>
            @endforelse
        </div>
    </div>
</section> -->